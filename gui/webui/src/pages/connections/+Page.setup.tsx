import { __render } from '@/shared/helper';
import ConnectionsPage from './connections-page.setup';

defineOptions({ name: 'ConnectionsRoutePage' });

export default __render(() => <ConnectionsPage />);
